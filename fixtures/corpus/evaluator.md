# Independent evaluator

The evaluator runs in a fresh context. It did not produce the work. It returns JSON with verdict pass, warn, or fail plus issues. Pass requires overall at least 7.0 and no high-severity issues. Warn is advisory unless the project promotes it. Fail blocks delivery of the user-facing answer.
